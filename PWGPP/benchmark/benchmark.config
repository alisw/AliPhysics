## default OCDB, if autoOCDB is set put in any 4 digit year starting with 20
#
defaultOCDB="local:///cvmfs/alice.gsi.de/alice/data/2010/OCDB/"

## by default the OCDB year set automatically for each chunk
## based on the path
## to disable set autoOCDB=0
#
#autoOCDB=0

## makeflow options
makeflowPath="/hera/alice/aux/cctools/bin"
#makeflowOptions="-T wq -N alice -d all -C ali-copilot.cern.ch:9097"
#makeflowOptions="-T wq -N alice -C ali-copilot.cern.ch:9097"
#makeflowOptions="-T wq -p 9123"
makeflowOptions="-T sge"
createWorkqueueCommand="/usr/bin/qsub -e batchLogs -o batchLogs -cwd -t 1:1000 -b y ${makeflowPath}/work_queue_worker -t 36000 $HOSTNAME 9123"
#createWorkqueue=1
batchCommand="/usr/bin/qsub"
batchFlags=""

## Directory to store output, productionID from command line will be appended
#
baseOutputDirectory="/hera/alice/mkrzewic/benchmarkDev/output"

## aliroot (will be sourced - for backwards compatibility only)
#
setupAliROOTenvInCurrentShell="/hera/alice/mkrzewic/alisoft/balice_TPCdev.sh"
#run QA with a known good version of the QA script
trustedQAtrainMacro='/hera/alice/mkrzewic/gsisvn/Calibration/QAtrain_duo.C'

## or (if set this will be the default, $setupAliROOTenvInCurrentShell will be ignored):
#
#alirootEnv="/cvmfs/alice.cern.ch/bin/alienv setenv AliRoot/v5-04-34-AN -c"

## this script makes the qa plots, it takes following arguments:
## ##### $1: LHC data period (used as an identifier to create an output path)
## ##### $2: Reconstruction pass (or some other identifier, used in the output path)
## ##### $3: The file list (relative or absolute path)
## ##### $4: The ouptut directory where the results will be stored (relative or absolute path)
#
qaPlotsScript="/hera/alice/aux/scripts/qaplots/MakeTPCQA.sh"

## reconstruct in /tmp, then copy results to final place
#
#reconstructInTemporaryDir=1

## trigger mask sent to the reconstruction
## if you want to send an empty string use "\"\""
## usually, for filtered data use empty, 
## for unfiltered chunks use kCalibBarrel
#
#recoTriggerOptions="?Trigger=kCalibBarrel"
#
recoTriggerOptions="\"\""

## merging is started when this percentage of input files is reconstructed
#
percentProcessedFilesToContinue=100
maxSecondsToWait=$(( 3600*24 ))

## maximal number of events to process (per chunk, -1 means no limit)
#
nEvents=50

## max no of chunks per run(0 means no limit)
#
nMaxChunks=0

## run these command(s) after the dir has been set up, use to e.g.
## modify the scripts on the fly (default QAtrain_duo.C needs modifications due
## to hardcoded raw OCDB storage)
#
postSetUpActionCPass0=""
postSetUpActionCPass1="sed -i 's/doCDBconnect\s*=\s*1/doCDBconnect = 0/' QAtrain_duo.C"

## detailed setting what to run
## CPass0
#
         runCPass0reco=1
runCPass0MergeMakeOCDB=1
## CPass1
#
         runCPass1reco=1
runCPass1MergeMakeOCDB=1

## filtering
       runESDfiltering=1
filteringFactorHighPt=1e2
filteringFactorV0s=1e1
filteringRunMacro="/hera/alice/mkrzewic/gsisvn/Calibration/runFilteringTask.C"

## create the QA plots per run
#
runMakeQAplots=1

## who to email upon completion
#
runMakeSummary=1
mailSummaryTo=""

## run a script to filter the logs, takes one argument, the base dir where to search for logs
#
logFilteringScript=/hera/alice/aux/scripts/benchmark/filterRecLog.sh

## uncomment to just run the logic, dont check for data and dont process it - 
## fake the output files (for debugging)
#
pretend=1

##dump all output to stdout for debugging
#
#dontRedirectStdOutToLog=1

## uncomment this option to force streaming the logs to the final destination instead of the running directory
## this way a log is always available even in case the batch system kills the job
#
#logToFinalDestination=1

#set the downscaling factors during the filtering fro expert QA (overrides the previous values)
#AliAnalysisTaskFilteredTree_fLowPtTrackDownscaligF="1e-2"
#AliAnalysisTaskFilteredTree_fLowPtV0DownscaligF="1e-2"

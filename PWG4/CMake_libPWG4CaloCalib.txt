# -*- mode: cmake -*-

set(SRCS
       CaloCalib/AliAnalysisTaskPHOSPi0CalibSelection.cxx 
)

# fill list of header files from list of source files
# by exchanging the file extension
String(REPLACE ".cxx" ".h" HDRS "${SRCS}")

AddLibrary(PWG4CaloCalib "${SRCS}" "${HDRS}")


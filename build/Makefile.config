# ROOT libraries 
ROOTLIBS      = $(shell $(ROOTSYS)/bin/root-config --glibs) -lMinuit -lHtml -lEG 
ROOTLIBS     := $(filter-out -lNew,$(ROOTLIBS))
ROOTLIBS  += -L/usr/X11R6/lib -lX11 

# Additional flags for building aliroot executable
EXEFLAGS = 

